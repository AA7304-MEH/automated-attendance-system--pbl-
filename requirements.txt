# Core (Phase 1-2)
numpy>=1.26
pillow>=10.0
opencv-python>=4.8
face-recognition>=1.3.0
pandas>=2.0

# Web app (Phase 3+)
flask>=3.0
flask-sqlalchemy>=3.1
flask-login>=0.6
gunicorn>=21.0

# CSV/analytics exports
pandas>=2.0

# NOTE: face-recognition installs dlib + face_recognition_models (~100 MB the
# first time). Building dlib from source needs CMake + a C++ toolchain
# (Windows: "pip install cmake" + Visual Studio Build Tools; macOS: brew install cmake).
# Inside this workspace, use scripts/setup_env.sh instead — it reuses the
# prebuilt wheel stashed in wheels/ and finishes in seconds.
