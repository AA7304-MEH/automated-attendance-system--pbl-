# Python
__pycache__/
*.pyc
venv/
.venv/

# Regenerable artifacts (scripts rebuild these in seconds)
data/face_encodings.pkl
data/outputs/_*.pkl

# Runtime web artifacts (recreated automatically)
data/attendance.db
data/outputs/web/
data/uploads/incoming/
.secret_key

# Never commit real student biometrics — enroll into a local, ignored folder
# when you switch from the AI-generated demo faces to real people:
# data/student_faces_real/

# rendered console-mode email digests
data/outputs/emails/
